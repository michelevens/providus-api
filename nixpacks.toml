[start]
cmd = "php artisan migrate --force && php artisan db:seed --force && php artisan reset-password admin@ennhealth.com 'EnnHealth2026!' || true && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
